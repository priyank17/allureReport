pipline{
	agent any
	stages{
		stage('build'){
		step{
				sh 'mvn clean test'
			}
		}
	}
	post{
		always{
			allure 'allure-results'
			deleteDir() /* clean up our workspace */
		}	
	}	
}